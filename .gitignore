# Never commit the local encrypted credential store or run logs
config/credentials.local.xml
config/credentials.json
logs/
*.jsonl
*.log

# Never commit the compiled binary
pax8tools.exe

# Downloaded Azure Automation module packages
Microsoft.Graph.*.zip
